# This file was created automatically by SWIG.
import MAppBasec

from MString import *

from MProfile import *

from MObject import *
class MAppBasePtr :
    def __init__(self,this):
        self.this = this
        self.thisown = 0
    def Exit(self,arg0):
        val = MAppBasec.MAppBase_Exit(self.this,arg0)
        return val
    def TopLevelFrame(self):
        val = MAppBasec.MAppBase_TopLevelFrame(self.this)
        return val
    def GetText(self,arg0):
        val = MAppBasec.MAppBase_GetText(self.this,arg0)
        return val
    def GetGlobalDir(self):
        val = MAppBasec.MAppBase_GetGlobalDir(self.this)
        val = StringPtr(val)
        val.thisown = 1
        return val
    def GetLocalDir(self):
        val = MAppBasec.MAppBase_GetLocalDir(self.this)
        val = StringPtr(val)
        val.thisown = 1
        return val
    def GetMimeManager(self):
        val = MAppBasec.MAppBase_GetMimeManager(self.this)
        return val
    def GetProfile(self):
        val = MAppBasec.MAppBase_GetProfile(self.this)
        val = ProfileBasePtr(val)
        return val
    def __repr__(self):
        return "<C MAppBase instance>"
class MAppBase(MAppBasePtr):
    def __init__(self,this):
        self.this = this






#-------------- FUNCTION WRAPPERS ------------------

def wxGetApp():
    val = MAppBasec.wxGetApp()
    val = MAppBasePtr(val)
    return val

MDialog_ErrorMessage = MAppBasec.MDialog_ErrorMessage

MDialog_SystemErrorMessage = MAppBasec.MDialog_SystemErrorMessage

MDialog_FatalErrorMessage = MAppBasec.MDialog_FatalErrorMessage

MDialog_Message = MAppBasec.MDialog_Message

MDialog_StatusMessage = MAppBasec.MDialog_StatusMessage

MDialog_YesNoDialog = MAppBasec.MDialog_YesNoDialog

def MDialog_FileRequester(arg0,*args):
    argl = map(None,args)
    try: argl[1] = argl[1].this
    except: pass
    try: argl[2] = argl[2].this
    except: pass
    try: argl[3] = argl[3].this
    except: pass
    try: argl[4] = argl[4].this
    except: pass
    try: argl[6] = argl[6].this
    except: pass
    args = tuple(argl)
    val = apply(MAppBasec.MDialog_FileRequester,(arg0.this,)+args)
    return val

def MInputBox(arg0,arg1,arg2,*args):
    val = apply(MAppBasec.MInputBox,(arg0.this,arg1,arg2,)+args)
    return val



#-------------- VARIABLE WRAPPERS ------------------

